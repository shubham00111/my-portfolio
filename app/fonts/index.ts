import { Inter, Lekton } from "next/font/google";

export const lekton = Lekton({
  subsets: ["latin-ext"],
  weight: ["400", "700"],
});

export const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "700"],
});
